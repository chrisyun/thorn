<?xml version="1.0" encoding="UTF-8"?>
<ehcache xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:noNamespaceSchemaLocation="../config/ehcache.xsd">

	<!-- 磁盘缓存文件 -->
	<diskStore path="java.io.tmpdir" />

	<!-- maxElementsInMemory: 内存管理的缓存元素数量最大限值 -->
	<!-- maxElementsOnDisk: 硬盘管理的缓存元素数量最大限值。默认值为0，就是没有限制-->
	<!-- eternal: 设定元素是否持久话。若设为true，则缓存元素不会过期。-->
	<!-- overflowToDisk: 设定是否在内存填满的时候把数据转到磁盘上-->
	<!-- timeToIdleSeconds： 设定元素在过期前空闲状态的时间，只对非持久性缓存对象有效。默认值为0,元素可以闲置至无限长时间。-->
	<!-- timeToLiveSeconds: 设定元素从创建到过期的时间。其他与timeToIdleSeconds类似-->
	<!-- diskPersistent: 设定在虚拟机重启时是否进行磁盘存储，默认为false-->
	<!-- diskExpiryThreadIntervalSeconds: 访问磁盘线程活动时间-->
	<!-- diskSpoolBufferSizeMB: 存入磁盘时的缓冲区大小，默认30MB,每个缓存都有自己的缓冲区-->
	<!-- memoryStoreEvictionPolicy: 元素逐出缓存规则。LRU最近最少使用，为默认，FIFO，LFU最少使用-->
	<defaultCache name="defaultCache" 
		maxElementsInMemory="10000"
		eternal="false"
		overflowToDisk="true" 
		maxElementsOnDisk="10000000" diskPersistent="false"
		diskExpiryThreadIntervalSeconds="120" memoryStoreEvictionPolicy="LRU" />
	
	<!-- security框架缓存用户对象 -->
	<cache name="securityCache" 
		timeToLiveSeconds="1800"
		maxElementsInMemory="500"
		overflowToDisk="true"
		maxElementsOnDisk="0" 
		eternal="false"
		diskExpiryThreadIntervalSeconds="30" 
		memoryStoreEvictionPolicy="LRU" />

</ehcache>
